# Audio I/O
sounddevice>=0.5.6
soundfile>=0.14.0

# Speech-to-Text (Vosk, offline FR)
vosk>=0.3.45

# Windows-specific (TTS SAPI + hotkey global)
pywin32>=306; sys_platform == "win32"

# Linux/WSL-specific (TTS pyttsx3 + eSpeak)
pyttsx3>=2.99; sys_platform != "win32"

# Hotkey cross-platform
pynput>=1.8.2

# Config
pyyaml>=6.0.2

# Tests
pytest>=9.1.1

# vosk transitive
requests>=2.31
